Return JSON for production automation with required fields account_id (string), action (one of suspend or retain), reason (string), and effective_date (YYYY-MM-DD). Validate every field and enum before use; on failure, return a structured error and do not apply the action.
